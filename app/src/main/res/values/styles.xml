<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="textColorPrimary">@color/colorAccent</item>-->
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" >
    </style>

    <style name="hint_text" parent="Base.TextAppearance.AppCompat.Light.Widget.PopupMenu.Small">>
        <item name="android:textColor">#808080</item>
        <item name="android:textSize">13sp</item>
    </style>

    <!--底部菜单样式-->
    <style name="TabButton" parent="NoButtonRadioButton">

        <item name="android:background">@null</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textColor">@drawable/tab_text_selector</item>
        <item name="android:paddingTop">4.5dp</item>
        <item name="android:paddingBottom">4.5dp</item>
        <item name="android:paddingLeft">0dp</item>
        <item name="android:drawablePadding">2dp</item>
        <item name="android:button">@null</item>
        <!-- 设置图与文字间距 -->
    </style>

    <style name="NoButtonRadioButton">
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
